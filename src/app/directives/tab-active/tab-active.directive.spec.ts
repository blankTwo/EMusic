import { TabActiveDirective } from './tab-active.directive';

describe('TabActiveDirective', () => {
  it('should create an instance', () => {
    const directive = new TabActiveDirective();
    expect(directive).toBeTruthy();
  });
});
